rules:
  - id: checkNextServiceCall
    message: "<pass>"
    severity: INFO
    languages:
      - go
    patterns:
      - pattern: total, err = mw.next.GetWholesaleTotal(partner, code, qty)
      - pattern-inside: func (mw instrumentingMiddleware) GetWholesaleTotal(partner string, code string, qty int) (total float64, err error) { ... }
