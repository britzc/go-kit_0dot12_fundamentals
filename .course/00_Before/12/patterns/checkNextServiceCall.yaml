rules:
  - id: checkNextCall
    message: "<pass>"
    severity: INFO
    languages:
      - go
    patterns:
      - pattern: total, err = mw.next.GetWholesaleTotal(partner, code, qty)
      - pattern-inside: func (mw loggingMiddleware) GetWholesaleTotal(partner string, code string, qty int) (total float64, err error) { ... }
