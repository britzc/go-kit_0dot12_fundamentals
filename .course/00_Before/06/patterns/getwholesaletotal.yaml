rules:
  - id: getwholesaletotal
    message: "<pass>"
    severity: INFO
    languages:
      - go
    pattern-either:
      - pattern: |
          func (ps *service) GetWholesaleTotal(partner string, code string, qty int) (total float64, err error) {...}
